# Project Rules

## RTK (Rust Token Killer)

**Always prefix commands with `rtk`** when running shell commands in this workspace to minimize token consumption.

Examples:
- `rtk git status`
- `rtk git diff`
- `rtk git log`
- `rtk cargo build`
- `rtk cargo test`
- `rtk ls`
